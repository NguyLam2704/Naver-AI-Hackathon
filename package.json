{
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "CI=false npm run build"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  }
}
